{
  "name": "empost",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "pack:check": "cd packages/emdash-plugin-md-draft && npm pack --dry-run && cd ../mcp-emdash-drafts && npm pack --dry-run",
    "emdash:conformance": "bash scripts/emdash-conformance.sh",
    "release:publish": "pnpm build && pnpm test && pnpm pack:check && node scripts/npm-auth-publish.mjs",
    "release:verify": "node scripts/verify-release.mjs",
    "release:github": "node scripts/create-github-release.mjs"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.11",
    "typescript": "^5.7.3"
  }
}
